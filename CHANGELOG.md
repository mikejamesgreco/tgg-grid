# Changelog

## Development Baseline - v1147

- Established this repository shell from the known-good TGG Grid v1147 build.
- API Explorer includes environments, local JSON Schema `$ref` handling, path parameters, custom request headers, custom query parameters, runtime authentication, and browser `fetch()` invocation.
- Sequence view includes groups/phases, request/response/async/note/self/destroy constructs, activation bars, PNG export, participant spacing, and `alt`/`else`/`opt`/`loop` fragments.
- Added generic public sample CSV and configuration files for repository use.
